<local:BaseWindow x:Class="glFTPd_Commander.Windows.SetAllotmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:glFTPd_Commander.Windows"
        Title="Set Weekly Allotment"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <StackPanel Margin="10">
        <Label Content="Section:"/>
        <TextBox x:Name="sectionText" PreviewTextInput="AmountInput"/>

        <Label Content="Amount:" Margin="0,10,0,0"/>
        <TextBox x:Name="amountText" PreviewTextInput="AmountInput"/>

        <Label Content="Units:" Margin="0,10,0,0"/>
        <ComboBox x:Name="unitsComboBox" DisplayMemberPath="Display"/>

        <StackPanel Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="70" Margin="0,0,10,0" Click="OkButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="70" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</local:BaseWindow>
