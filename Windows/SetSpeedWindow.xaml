<local:BaseWindow x:Class="glFTPd_Commander.Windows.SetSpeedWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:glFTPd_Commander.Windows"
        Title="Set Speed"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="Speed:"/>
            <TextBox x:Name="SpeedTextBox" PreviewTextInput="SpeedInput"/>
        </StackPanel>

        <StackPanel Margin="0,0,0,10" Grid.Row="1">
            <TextBlock Text="Unit:"/>
            <ComboBox x:Name="UnitsComboBox" DisplayMemberPath="Display"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="5" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="75" Margin="5" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</local:BaseWindow>
