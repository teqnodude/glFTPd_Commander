<local:BaseWindow
    x:Class="glFTPd_Commander.Windows.SetMaxAllotWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:glFTPd_Commander.Windows"
    Title="Set Max Allotment"
    ResizeMode="NoResize"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label
            Grid.Row="0"
            Margin="0,0,10,0"
            Content="Amount:" />
        <TextBox
            x:Name="AmountTextBox"
            Grid.Row="1"
            Width="150"
            Margin="0,0,0,10" />

        <Label
            Grid.Row="2"
            Margin="0,0,10,0"
            Content="Units:" />
        <ComboBox
            x:Name="UnitsComboBox"
            Grid.Row="3"
            Width="150"
            Margin="0,0,0,10"
            DisplayMemberPath="Display"
            SelectedValuePath="Code" />

        <StackPanel
            Grid.Row="4"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="70"
                Margin="0,0,10,0"
                Click="OkButton_Click"
                Content="OK"
                IsDefault="True" />
            <Button
                Width="70"
                Click="CancelButton_Click"
                Content="Cancel"
                IsCancel="True" />
        </StackPanel>
    </Grid>

</local:BaseWindow>
